<!DOCTYPE HTML>
html(lang="es")
    head
        include partials/header
    body.login
        .contenedor-formulario 
            h1 Iniciar Sesión en UpTask

            //- if error
            //-     p.alerta.error= error

            if locals.mensajes
                .alertas 
                    each categoria in Object.keys(locals.mensajes)
                        each error in mensajes[categoria]
                            .alerta(class=`${categoria}`)
                                p= error

            form#formulario.caja.caja-login(action="/iniciar-sesion" method="POST") 
                .campo
                    label(for="email") E-mail
                    input(type="email" name="email" placeholder="Correo Electrónico" value= email)
                .campo
                    label(for="password") E-mail
                    input(type="password" name="password" placeholder="Contraseña" value= password)
                .campo.enviar 
                    input.boton(type="submit" value="Iniciar Sesión")
                .campo.acciones
                    a(href="/crear-cuenta") ¿Aún no tienes tu cuenta? Create una gratis
                    a(href="/reestablecer") ¿Olvidaste tu Clave?